package ObjectDeclarationAndInitializationInJava;

public class ObjectDeclaration {
    //The process of defining a reference variable of a class type is called object declaration in Java.
    // The general syntax to declare a reference object of class type is as:
    //classname ObjectName;

    ObjectDeclaration obj;

    //ObjectDeclaration is the name of the class, and c is the reference variable.
    //At this stage, no object is created.
    //Only memory for the reference variable is allocated, and it refers to no object. It contains null by default.
}
